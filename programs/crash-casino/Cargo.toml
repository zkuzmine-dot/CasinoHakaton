[package]
name = "crash-casino"
version = "0.1.0"
description = "Provably fair Crash Casino on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "crash_casino"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = { version = "0.29.0", features = ["init-if-needed"] }
